//next.js does a few things for us
//domain.com/news is automatically set up as routing inside the pages folder
//import React is added behind the scenes

const NewsPage = () => {
    return <h1>The News Page</h1>
};

export default NewsPage;